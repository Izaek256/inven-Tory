/**
 * @inven-tory/shared-types
 *
 * Shared TypeScript type definitions for INVENTORY Tory v1.1.0.
 * Used by apps/desktop, apps/web, and apps/mobile.
 *
 * Types are added in later issues:
 *   Issue 02: Core domain types (Store, Product, InventoryTransaction, etc.)
 *   Issue 08: Auth types (LoginRequest, TokenResponse, DeviceRegistration)
 *   Issue 09: Sync types (PushPayload, PullResponse, SyncStatus)
 */

// ---------------------------------------------------------------------------
// Placeholder — keeps the package importable from Issue 01 onwards.
// Replace this comment with actual type exports starting from Issue 02.
// ---------------------------------------------------------------------------

export type {};
