import React from 'react';

/**
 * Root component for the remote management dashboard.
 * Stub — notification center and global search implemented in Issue 11.
 */
function App(): React.ReactElement {
  return (
    <div>
      <h1>INVENTORY Tory — Dashboard</h1>
      <p>Remote dashboard stub (Issue 11 implements the full UI)</p>
    </div>
  );
}

export default App;
