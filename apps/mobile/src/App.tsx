import React from 'react';

/**
 * Root component for the read-only mobile companion.
 * FR-MOBILE-003: This app never provides transaction-entry controls.
 * Stub — implemented in Issue 20.
 */
function App(): React.ReactElement {
  return (
    <div>
      <h1>INVENTORY Tory — Mobile</h1>
      <p>Read-only mobile companion stub (Issue 20 implements the full UI)</p>
    </div>
  );
}

export default App;
